version: '2'

services:
  db:
    build:
      context: ./db
    environment:
      - POSTGRES_PASSWORD=6485882
    ports:
      - "5432:5432"
    volumes:
      - ./db/data:/var/lib/postgresql/data
  yidu:
    build:
      context: ./yidu
    ports:
      - 80:8080
    volumes:
        - ./yidu/ROOT:/usr/local/tomcat/webapps/ROOT


    